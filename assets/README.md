# Assets

El logo oficial vive en `logo-datametrics.png` (fondo transparente) y se usa
automáticamente en el header y en `st.logo()` (sidebar) — ver `ui/header.py`
y `ui/layout.py`. Mientras el archivo no exista, el header muestra un
wordmark de texto "DATA METRICS" como respaldo.

No redimensionar, recortar ni recolorear el logo: se usa tal cual fue
provisto por DATA METRICS — Business Intelligence and Analytics.
