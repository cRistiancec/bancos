# Assets

Coloca aquí el logo oficial de COSEDE como `logo_cosede.png` (fondo transparente,
alto recomendado ~120px) para que aparezca automáticamente en el header de la
plataforma. Mientras el archivo no exista, el header muestra un wordmark de
texto "COSEDE" como respaldo — ver `ui/header.py`.
